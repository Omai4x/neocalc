<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ backup and transfer rules. See backup_rules.xml for why cloud
  backup carries nothing.

  Device-to-device transfer is treated differently on purpose: it is a direct
  handset-to-handset copy that never touches a server, so it does not conflict
  with the "never uploaded" promise, and it is exactly what someone changing
  phone expects to happen.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
    </cloud-backup>
    <device-transfer>
        <include domain="sharedpref" path="." />
    </device-transfer>
</data-extraction-rules>
